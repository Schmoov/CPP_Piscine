#include <exception>
#include <iostream>
#include "Bureaucrat.hpp"
#include "AForm.hpp"

int main() {}
/*

int main() {
	try {
		Bureaucrat Hermes("Hermes", 18);
		std::cout << Hermes;
		Form f1("f1", 50, 2);
		Form f2("f2", 2, 50);
		std::cout << f1 << f2;
		Hermes.signForm(f1);
		Hermes.signForm(f1);
		Hermes.signForm(f2);
	} catch (std::exception & e) {
		std::cout << "Oops \n";
	}
	try {
		Form f3("f3", 253, 2);
	} catch (std::exception & e) {
		std::cout << "Oops \n";
	}
}
*/
